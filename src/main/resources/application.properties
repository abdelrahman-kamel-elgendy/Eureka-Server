# =============================
# EUREKA SERVER - CONFIGURATION
# =============================

# Application Configuration
spring.application.name=${APP_NAME}
server.port=${SERVER_PORT}

# Eureka Server Configuration
eureka.client.register-with-eureka=${EUREKA_CLIENT_REGISTER_WITH_EUREKA}
eureka.client.fetch-registry=${EUREKA_CLIENT_FETCH_REGISTRY}
eureka.server.enable-self-preservation=${EUREKA_SERVER_ENABLE_SELF_PRESERVATION}
eureka.server.eviction-interval-timer-in-ms=${EUREKA_SERVER_EVICTION_INTERVAL_TIMER_MS}
eureka.server.renewal-percent-threshold=${EUREKA_SERVER_RENEWAL_PERCENT_THRESHOLD}
eureka.server.wait-time-in-ms-when-sync-empty=0

# Disable peer replication for standalone mode
eureka.server.peer-node-connect-timeout-ms=1000
eureka.server.peer-node-read-timeout-ms=1000
eureka.server.peer-node-total-connections=10
eureka.server.peer-node-total-connections-per-host=10

# Eureka Instance Configuration
eureka.instance.hostname=${EUREKA_INSTANCE_HOSTNAME}

# Actuator Configuration
management.endpoints.web.exposure.include=${MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE}
management.endpoint.health.show-details=${MANAGEMENT_ENDPOINT_HEALTH_SHOW_DETAILS}
management.info.env.enabled=true

# Logging Configuration
logging.level.com.netflix.eureka=${LOGGING_LEVEL_COM_NETFLIX_EUREKA}
logging.level.com.netflix.discovery=${LOGGING_LEVEL_COM_NETFLIX_DISCOVERY}

# Security Configuration (for SecurityConfig)
app.security.admin.username=${EUREKA_USERNAME}
app.security.admin.password=${EUREKA_PASSWORD}
app.security.client.username=${SPRING_SECURITY_USER_NAME}
app.security.client.password=${SPRING_SECURITY_USER_PASSWORD}

# Disable compatibility check
spring.cloud.compatibility-verifier.enabled=${SPRING_CLOUD_COMPATIBILITY_VERIFIER_ENABLED}
